stages:
  - artifacts

artifacts:
  stage: artifacts
  tags: [ stage, shell]
  script:
    - make artifacts
  artifacts:
    public: true
    paths:
      - machines/**/*.sh
      - scripts/bash/*.sh
      - scripts/powershell/MdctecMaintenance/MdctecMaintenance.zip
