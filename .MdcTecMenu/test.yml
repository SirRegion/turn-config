items:
  ComplianceBase Utilities:
    items:
      Pull a docker image:
        environment:
          MTEC_DOCKER_REGISTRY:
            persistent: "Yes"
          CB_DOCKER_CONTAINER:
            persistent: "Yes"
          CB_VARIANT:
            persistent: "Yes"
          CB_VERSION:
            persistent: "Yes"
        powershell:
          - docker pull "${Env:MTEC_DOCKER_REGISTRY}/main/${Env:CB_DOCKER_CONTAINER}/${Env:CB_VARIANT}:${Env:CB_VERSION}"
      Inspect:
        powershell:
          - echo "not implemented"
      Configure:
        powershell:
          - echo "not implemented"
      Utilities:
        items:
          Push Files to the Container:
            powershell:
              - echo "not implemented"
          Pull Files from Container:
            powershell:
              - echo "not implemented"
          Open an interactive Shell inside the Container (broken):
            environment:
              CB_DOCKER_CONTAINER:
                persistent: "Yes"
            powershell:
              - docker exec -it $Env:CB_DOCKER_CONTAINER powershell
          Create Data Backup:
            powershell:
              - echo "not implemented"
      Run:
        powershell:
          - echo "not implemented"
      Stop:
        powershell:
          - echo "not implemented"